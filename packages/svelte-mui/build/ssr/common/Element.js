'use strict';

var index = require('../index-4eebdc97.js');

/* src/coffee/common/Element.svelte generated by Svelte v3.29.4 */

const Element = index.create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let { tag } = $$props;
	let element;
	let isNative;

	if (typeof tag === "string") {
		isNative = true;
	} else {
		isNative = false;
	}

	// $: props = $$props
	index.onMount(() => {
		if (!isNative) {
			return;
		}

		// let element = document.querySelectorAll('.element')[0]
		let newElement = document.createElement(tag.toString() || "div");

		newElement.innerHTML = element.innerHTML;
		element.parentNode.insertBefore(newElement, element.nextSibling);
		element.remove();
	});

	if ($$props.tag === void 0 && $$bindings.tag && tag !== void 0) $$bindings.tag(tag);

	return `${isNative
	? `<div${index.spread([$$props])}${index.add_attribute("this", element, 1)}>${slots.default ? slots.default({}) : ``}</div>`
	: `${index.validate_component(tag || index.missing_component, "svelte:component").$$render($$result, Object.assign($$props), {}, {
			default: () => `${slots.default ? slots.default({}) : ``}`
		})}`}`;
});

module.exports = Element;
